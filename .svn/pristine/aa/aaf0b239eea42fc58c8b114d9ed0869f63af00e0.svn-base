/**
 * Created by xxx on 2017/3/6.
 * 任务巡店地图展示
 */
import React, {Component} from 'react';
import {
    StyleSheet,
    View,
    Button
} from 'react-native';
var { requireNativeComponent } = require('react-native');
// requireNativeComponent 自动把这个组件提供给 "RNTMapManager"
 module.exports = requireNativeComponent('BDMapView', null);
// var RNTMap = requireNativeComponent('BDMapView', null);
export default class MapComponent extends Component {
    render(){

        alert('地图组件');
        return(
            <BDMapView {...this.props} />
        )
    }
}
const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: 'white'
    },
});